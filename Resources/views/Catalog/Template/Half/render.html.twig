{% extends theme %}

{% import '@EkynaProduct/Catalog/Template/macros.html.twig' as macros %}

{% block content -%}
{% apply spaceless %}
{% for slot in page.slots|slice(0, 2) -%}
    {% set product = slot.product %}
    <div class="product half {{ loop.first ? 'left' : 'right' }}">
        <div class="inner">
            <div class="row">
            {% if loop.first -%}
                <div class="col-xs-6">{{ macros.image(catalog, product) }}</div>
            {%- endif %}
                <div class="col-xs-6">
                    {{ macros.title(catalog, product) }}
                    <div class="row">
                        <div class="col-xs-6">
                            {{ macros.infos(catalog, product) }}
                        </div>
                        <div class="col-xs-6">
                            {{ macros.grid(catalog, product) }}
                        </div>
                    </div>
                </div>
            {% if not loop.first -%}
                <div class="col-xs-6">{{ macros.image(catalog, product) }}</div>
            {%- endif %}
            </div>
            <div class="row">
                <div class="col-xs-12">
                    <p class="description">{{ product.description|paragraph_to_break }}</p>
                </div>
            </div>
        </div>
    </div>
{%- endfor %}
{% endapply %}
{% endblock content %}
