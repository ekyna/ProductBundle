{{ show_row(page.options.title, 'text', {
    'label': 'ekyna_core.field.title'
}) }}
{{ show_row(page.options.description, 'textarea', {
    'label': 'ekyna_core.field.description',
    'html': true
}) }}
{{ show_row(page.options.image, 'media', {
    'label': 'ekyna_core.field.image'
}) }}
