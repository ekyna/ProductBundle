{{ show_row(page.options.title, 'text', {
    'label': t('field.title', [], 'EkynaUi'),
}) }}
{{ show_row(page.options.description, 'textarea', {
    'label': t('field.description', [], 'EkynaUi'),,
    'html': true
}) }}
{{ show_row(page.options.image, 'media_media', {
    'label': t('field.image', [], 'EkynaUi'),
}) }}
