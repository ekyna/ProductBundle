{% extends "EkynaAdminBundle:Entity/Default:show.html.twig" %}

{% block main %}
<div class="actions">
    {{ admin_resource_btn(brand, 'edit') }}
    {{ admin_resource_btn(brand, 'remove') }}
</div>
<div class="show show-with-tabs">
    <ul class="nav nav-tabs" data-tab-key="{{ resource_id }}">
        <li class="active">
            <a href="#brand-general" id="toggle-general" data-toggle="tab">
                {{ 'ekyna_core.field.general'|trans }}</a>
        </li>
        <li>
            <a href="#brand-seo" id="toggle-seo" data-toggle="tab">
                {{ 'ekyna_core.field.seo'|trans }}
            </a>
        </li>
    </ul>
    <div class="show-body">
        <div class="tab-content">
            <div class="tab-pane active" id="brand-general">
                <div class="row">
                    <div class="col-md-6">
                        {{ show_row(brand.name, 'text', 'ekyna_core.field.name', {'label_nb_col': 4, 'widget_nb_col': 8}) }}
                    </div>
                    <div class="col-md-6">
                        {{ show_row(brand.visible, 'checkbox', 'ekyna_core.field.visible', {'label_nb_col': 4, 'widget_nb_col': 8}) }}
                    </div>
                </div>
                {{ show_translations_row(brand.translations, {
                    'fields': {
                        'title': {'label': 'ekyna_core.field.title'},
                        'description': {
                            'label': 'ekyna_core.field.description',
                            'type': 'tinymce',
                            'content': path('ekyna_product_brand_admin_tinymce', {'brandId': brand.id}),
                            'options': {'height': 100}
                        }
                    }
                }) }}
                {{ show_row(brand.media, 'media', 'ekyna_core.field.image') }}
            </div>
            <div class="tab-pane" id="brand-seo">
                {{ show_row(brand.seo, 'seo') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
