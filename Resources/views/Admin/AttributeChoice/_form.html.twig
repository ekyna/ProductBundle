{% extends '@EkynaAdmin/Entity/Crud/_form_default.html.twig' %}

{% block main_contents -%}
    <div class="tab-pane active" id="{{ resource_name }}-general">
        {{ form_row(form.attribute) }}
        {{ form_row(form.name) }}
        {{ form_row(form.translations, {'label_col': 0, 'widget_col': 12}) }}
        <div class="row">
            <div class="col-md-6">
                {{ form_row(form.media, {'label_col': 4, 'widget_col': 8}) }}
            </div>
            <div class="col-md-6">
                {{ form_row(form.color, {'label_col': 4, 'widget_col': 8}) }}
            </div>
        </div>
    </div>
{%- endblock main_contents -%}
