{% extends "EkynaCmsBundle::base.html.twig" %}

{% block metas %}
    {{ cms_metas(product) }}
{% endblock metas %}

{% block content %}

    <div class="container">
        <h1>{{ product.designation }}</h1>
        <hr>

        {{ form_start(form) }}
        <div class="form-body">
            {{ form_widget(form) }}
            {{ form_rest(form) }}
        </div>
        {{ form_end(form) }}
    </div>

    {{ cms_content(product) }}

{% endblock content %}
